query PostQuery($amount: Int!, $offset: Int!) {
    posts(amount: $amount, offset: $offset) {
        id
        players
        minRank
        gameMode
        needed
        createdAtEpochSecond
    }
}